{% extends 'base.html.twig' %}

{% block title %}Picture{% endblock %}

{% block body %}
    <h1>Picture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picture.id }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ picture.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_picture_indexpictures', {'id': dog.id}) }}">back to list</a>

    <a href="{{ path('app_back_picture_edit', {'id': picture.id, dog_id: dog.id}) }}">edit</a>

    {{ include('back/picture/_delete_form.html.twig') }}
{% endblock %}
