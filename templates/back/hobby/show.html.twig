{% extends 'base.html.twig' %}

{% block title %}Hobby{% endblock %}

{% block body %}
    <h1>Hobby</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hobby.id }}</td>
            </tr>
            <tr>
                <th>Hobby</th>
                <td>{{ hobby.hobby }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_hobby_index') }}">back to list</a>

    <a href="{{ path('app_back_hobby_edit', {'id': hobby.id}) }}">edit</a>

    {{ include('back/hobby/_delete_form.html.twig') }}
{% endblock %}
