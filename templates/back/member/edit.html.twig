{% extends 'back/base_back.html.twig' %}

{% block title %}Editer le membre{% endblock %}

{% block body %}
    <h1>Editer le membre</h1>

    {{ include('back/member/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="btn btn-warning float-end" href="{{ path('app_back_member_index') }}">Retour à la liste</a>

    {{ include('back/member/_delete_form.html.twig') }}
{% endblock %}
